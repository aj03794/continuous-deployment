export const unzipApp = ({
    logger
}) => () => new Promise((resolve, reject) => {

    logger.info({
        function: 'unzipApp',
        params: {
            
        }
    })

    resolve()

})